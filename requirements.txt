# Core
anthropic>=0.18.0
openai>=1.12.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0

# API (for n8n / HTTP trigger)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
psycopg2-binary>=2.9.9
supabase>=2.3.0

# YouTube & media
google-api-python-client>=2.111.0
# Latest stable yt-dlp (2026.3.17) is bot-blocked by YouTube's current player.
# Stable channel has not shipped a newer release; pin the newest nightly (prerelease)
# which carries the up-to-date YouTube extractor. Exact == pin is required so pip
# will install the prerelease (a >= constraint excludes .dev builds per PEP 440).
yt-dlp==2026.5.25.234532.dev0
youtube-transcript-api>=1.0.0

# Transcription
deepgram-sdk>=5.0.0

# Auth (Supabase JWT for private search)
PyJWT>=2.8.0

# Build cache bust: 2026-06-01 (yt-dlp nightly pin)
