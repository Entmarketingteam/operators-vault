# Core
anthropic>=0.18.0
openai>=1.12.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0

# API (for n8n / HTTP trigger)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
psycopg2-binary>=2.9.9
supabase>=2.3.0

# YouTube & media
google-api-python-client>=2.111.0
# yt-dlp must stay current or YouTube's player will bot-block the extractor
# (this happened 2026-05-28 on the stale 2026.3.17 stable pin). Stable has since
# shipped 2026.6.9 with an up-to-date extractor, so we can drop the old nightly
# (.dev0) prerelease hack and floor on the current stable. Bump this floor (or
# rebuild) whenever YouTube breaks the extractor again.
yt-dlp>=2026.6.9
youtube-transcript-api>=1.0.0

# Transcription
deepgram-sdk>=5.0.0

# Auth (Supabase JWT for private search)
PyJWT>=2.8.0

# Build cache bust: 2026-06-16 (yt-dlp -> stable 2026.6.9, dropped nightly pin)
